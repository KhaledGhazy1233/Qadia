<UserControl x:Class="Qadia.Views.SessionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1200"
             FlowDirection="RightToLeft">
             
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#10B981"/>
        <SolidColorBrush x:Key="HoverGreen" Color="#059669"/>
        <SolidColorBrush x:Key="TextDark" Color="#1F2937"/>
        <SolidColorBrush x:Key="TextGray" Color="#6B7280"/>
        <SolidColorBrush x:Key="BorderGray" Color="#E5E7EB"/>

        <Style x:Key="ModernCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" Opacity="0.03" BlurRadius="15" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentGreen}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bgBorder" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bgBorder" Property="Background" Value="{StaticResource HoverGreen}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SessionsDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F9FAFB"/>
                        <Setter Property="Foreground" Value="{StaticResource TextGray}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="16,12"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="RowStyle">
                <Setter.Value>
                    <Style TargetType="DataGridRow">
                        <Setter Property="MinHeight" Value="80"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F9FAFB"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="16,8" Background="Transparent">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Dialog Styles -->
        <SolidColorBrush x:Key="DialogHeaderBg" Color="White"/>
        <SolidColorBrush x:Key="DialogBodyBg" Color="White"/>
        <SolidColorBrush x:Key="ErrorRed" Color="#EF4444"/>
        <SolidColorBrush x:Key="SuccessGreen" Color="#059669"/>

        <Style x:Key="DialogTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#10B981"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DialogLabel" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- Main Content -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#F9FAFB">
            <StackPanel Margin="32">
                
                <!-- Header Section -->
                <Grid Margin="0,0,0,32">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel>
                        <TextBlock Text="سجل الجلسات العام" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextDark}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <TextBlock Text="الرئيسية" FontSize="14" Foreground="{StaticResource TextGray}"/>
                            <TextBlock Text=" / " FontSize="14" Foreground="{StaticResource TextGray}"/>
                            <TextBlock Text="إدارة الجلسات" FontSize="14" Foreground="{StaticResource TextGray}" FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                        <Border Background="White" BorderBrush="{StaticResource BorderGray}" BorderThickness="1" CornerRadius="8" Padding="12,0" Margin="0,0,12,0">
                            <Grid Height="40">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="01 يناير 2024 - 30 يناير 2024" VerticalAlignment="Center" Foreground="{StaticResource TextDark}" Margin="0,0,12,0"/>
                                <TextBlock Grid.Column="1" Text="📅" VerticalAlignment="Center" Foreground="{StaticResource TextGray}"/>
                            </Grid>
                        </Border>
                        <Button Style="{StaticResource ActionButton}" Command="{Binding NewSessionCommand}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="+" FontSize="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                <TextBlock Text="إضافة جلسة يدوية" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

                <!-- Filter Tabs -->
                <Border Background="White" CornerRadius="8" Padding="4" HorizontalAlignment="Right" Margin="0,0,0,32" BorderBrush="{StaticResource BorderGray}" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <Button Content="الكل" Width="80" Height="36" Background="#1F2937" Foreground="White" BorderThickness="0"/>
                        <Button Content="قيد الانتظار" Width="100" Height="36" Background="Transparent" Foreground="{StaticResource TextGray}" BorderThickness="0"/>
                        <Button Content="تمت" Width="80" Height="36" Background="Transparent" Foreground="{StaticResource TextGray}" BorderThickness="0"/>
                    </StackPanel>
                </Border>

                <!-- Main Table Section -->
                <Border Style="{StaticResource ModernCard}" Padding="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <DataGrid Grid.Row="0" ItemsSource="{Binding Sessions}" Style="{StaticResource SessionsDataGrid}">
                            <DataGrid.Columns>
                                <!-- Date and Time -->
                                <DataGridTemplateColumn Header="التاريخ والوقت" Width="1.5*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{Binding SessionDateString}" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextDark}"/>
                                                <TextBlock Text="{Binding SessionTimeString}" FontSize="12" Foreground="{StaticResource TextGray}" Margin="0,4,0,0"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- Case and Client -->
                                <DataGridTemplateColumn Header="القضية والموكل" Width="2*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{Binding LawsuitNumber}" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextDark}"/>
                                                <TextBlock Text="{Binding ClientName}" FontSize="12" Foreground="{StaticResource TextGray}" Margin="0,4,0,0"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- Requirements -->
                                <DataGridTemplateColumn Header="المتطلبات" Width="2*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ItemsControl ItemsSource="{Binding Requirements}">
                                                <ItemsControl.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel Orientation="Horizontal" VerticalAlignment="Center"/>
                                                    </ItemsPanelTemplate>
                                                </ItemsControl.ItemsPanel>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Background="#F1F5F9" CornerRadius="4" Padding="8,4" Margin="0,0,8,4">
                                                            <TextBlock Text="{Binding}" FontSize="11" Foreground="#475569"/>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- Decision -->
                                <DataGridTemplateColumn Header="القرار" Width="1.2*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="{Binding DecisionColor}" CornerRadius="12" Padding="12,4" HorizontalAlignment="Center">
                                                <TextBlock Text="{Binding Decision}" FontSize="12" FontWeight="Bold" Foreground="{Binding DecisionTextColor}"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- Status Icon -->
                                <DataGridTemplateColumn Header="الحالة" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Grid Width="24" Height="24" HorizontalAlignment="Center">
                                                <Ellipse Stroke="{Binding StatusColor}" StrokeThickness="2"/>
                                                <TextBlock Text="✓" Foreground="{Binding StatusColor}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="12"/>
                                            </Grid>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- Actions -->
                                <DataGridTemplateColumn Header="الإجراءات" Width="150">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Background="Transparent" BorderThickness="0" Margin="8,0" Cursor="Hand" ToolTip="تعديل">
                                                    <TextBlock Text="✏️" FontSize="16" Foreground="{StaticResource TextGray}"/>
                                                </Button>
                                                <Button Background="Transparent" BorderThickness="0" Margin="8,0" Cursor="Hand" ToolTip="السجل">
                                                    <TextBlock Text="🕒" FontSize="16" Foreground="{StaticResource TextGray}"/>
                                                </Button>
                                                <Button Background="Transparent" BorderThickness="0" Margin="8,0" Cursor="Hand" ToolTip="حذف">
                                                    <TextBlock Text="🗑️" FontSize="16" Foreground="#EF4444"/>
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Pagination -->
                        <Border Grid.Row="1" Padding="24" BorderBrush="{StaticResource BorderGray}" BorderThickness="0,1,0,0">
                            <Grid>
                                <TextBlock Text="عرض 1-10 من أصل 48 جلسة" Foreground="{StaticResource TextGray}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                    <Button Content="&gt;" Width="32" Height="32" Margin="4,0" Background="White" BorderBrush="{StaticResource BorderGray}"/>
                                    <Button Content="1" Width="32" Height="32" Margin="4,0" Background="#1F2937" Foreground="White" BorderThickness="0"/>
                                    <Button Content="2" Width="32" Height="32" Margin="4,0" Background="White" BorderBrush="{StaticResource BorderGray}"/>
                                    <Button Content="3" Width="32" Height="32" Margin="4,0" Background="White" BorderBrush="{StaticResource BorderGray}"/>
                                    <Button Content="&lt;" Width="32" Height="32" Margin="4,0" Background="White" BorderBrush="{StaticResource BorderGray}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>

                <!-- Stats Row -->
                <Grid Margin="0,32,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="24"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="24"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Today Sessions -->
                    <Border Grid.Column="4" Style="{StaticResource ModernCard}" Padding="20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="جلسات اليوم" FontSize="14" Foreground="{StaticResource TextGray}"/>
                                <TextBlock Text="{Binding SessionsToday, StringFormat=\{0:D2\}}" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextDark}" Margin="0,8,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Width="48" Height="48" Background="#1F2937" CornerRadius="8">
                                <TextBlock Text="📅" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Pending Decisions -->
                    <Border Grid.Column="2" Style="{StaticResource ModernCard}" Padding="20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="بانتظار القرار" FontSize="14" Foreground="{StaticResource TextGray}"/>
                                <TextBlock Text="{Binding PendingDecisions}" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextDark}" Margin="0,8,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Width="48" Height="48" Background="#4ADE80" CornerRadius="8">
                                <TextBlock Text="📋" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Monthly Completed -->
                    <Border Grid.Column="0" Style="{StaticResource ModernCard}" Padding="20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="اكتملت هذا الشهر" FontSize="14" Foreground="{StaticResource TextGray}"/>
                                <TextBlock Text="{Binding CompletedThisMonth}" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextDark}" Margin="0,8,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Width="48" Height="48" Background="#DBEAFE" CornerRadius="8">
                                <TextBlock Text="☑️" Foreground="#1E40AF" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <!-- Pop-up Overlay (Add Session) -->
        <Grid x:Name="Overlay" Visibility="{Binding IsAddSessionDialogOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Background="Black" Opacity="0.5"/>
            
            <Border Background="White" CornerRadius="12" Width="650" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.25"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Header -->
                        <RowDefinition Height="*"/>    <!-- Body -->
                        <RowDefinition Height="Auto"/> <!-- Footer -->
                    </Grid.RowDefinitions>

                    <!-- Header Section -->
                    <Border Grid.Row="0" Background="#1F2937" CornerRadius="12,12,0,0" Padding="24,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel>
                                <TextBlock Text="إضافة جلسة جديدة" Foreground="White" FontSize="18" FontWeight="Bold"/>
                                <TextBlock Text="أدخل تفاصيل الجلسة القانونية للمتابعة" Foreground="#9CA3AF" FontSize="12" Margin="0,4,0,0"/>
                            </StackPanel>

                            <Button Grid.Column="1" Content="✕" Foreground="White" Background="Transparent" BorderThickness="0" FontSize="18" VerticalAlignment="Top" Margin="0,-10,-10,0" Cursor="Hand" Command="{Binding CloseDialogCommand}"/>
                            

                        </Grid>
                    </Border>

                    <!-- Body -->
                    <ScrollViewer Grid.Row="1" MaxHeight="600" VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="24,32">
                            <!-- Case & Client Row -->
                            <Grid Margin="0,0,0,24">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Case Selection -->
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="اختيار القضية" Style="{StaticResource DialogLabel}"/>
                                    <Grid>
                                        <TextBox Style="{StaticResource DialogTextBox}" Text="{Binding TargetCase}" Padding="12,10,40,10"/>
                                        <TextBlock Text="🔍" Foreground="{StaticResource TextGray}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                    </Grid>
                                </StackPanel>

                                <!-- Client Name -->
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="اسم الموكل" Style="{StaticResource DialogLabel}"/>
                                    <TextBox Style="{StaticResource DialogTextBox}" Text="{Binding TargetClient}" IsReadOnly="True" Background="#F9FAFB"/>
                                </StackPanel>
                            </Grid>

                            <!-- Date & Time Row -->
                            <Grid Margin="0,0,0,24">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Session Date -->
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="تاريخ الجلسة" Style="{StaticResource DialogLabel}"/>
                                    <Grid>
                                        <TextBox Style="{StaticResource DialogTextBox}" Text="{Binding TargetDate, StringFormat=\{0:MM/dd/yyyy\}}" Padding="12,10,40,10"/>
                                        <TextBlock Text="📅" Foreground="{StaticResource TextGray}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                    </Grid>
                                </StackPanel>

                                <!-- Session Time -->
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="وقت الجلسة" Style="{StaticResource DialogLabel}"/>
                                    <Grid>
                                        <TextBox Style="{StaticResource DialogTextBox}" Text="{Binding TargetTime}" Padding="12,10,40,10"/>
                                        <TextBlock Text="🕒" Foreground="{StaticResource TextGray}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                    </Grid>
                                </StackPanel>
                            </Grid>

                            <!-- Requirements -->
                            <StackPanel Margin="0,0,0,24">
                                <TextBlock Text="المتطلبات" Style="{StaticResource DialogLabel}"/>
                                <TextBox Style="{StaticResource DialogTextBox}" Text="{Binding TargetRequirements}" Height="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top" Tag="أدخل متطلبات الجلسة والمستندات اللازمة..."/>
                            </StackPanel>

                            <Separator Background="{StaticResource BorderGray}" Margin="0,8,0,24"/>

                            <!-- Decision Type Row -->
                            <Grid Margin="0,0,0,24">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Edit Mode Pill -->
                                <Border Grid.Column="0" Background="#DCFCE7" CornerRadius="4" Padding="8,4" VerticalAlignment="Bottom" Margin="0,0,12,10">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="✎" Foreground="#15803D" Margin="0,0,4,0"/>
                                        <TextBlock Text="وضع التحرير نشط" Foreground="#15803D" FontSize="11" FontWeight="Bold"/>
                                    </StackPanel>
                                </Border>

                                <!-- Decision Type -->
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="نوع القرار" Style="{StaticResource DialogLabel}"/>
                                    <TextBox Style="{StaticResource DialogTextBox}" Text="{Binding TargetDecisionType}" Tag="تأجيل"/>
                                </StackPanel>
                            </Grid>

                            <!-- Decision Details -->
                            <StackPanel Margin="0,0,0,8">
                                <TextBlock Text="القرار" Style="{StaticResource DialogLabel}"/>
                                <TextBox Style="{StaticResource DialogTextBox}" Text="{Binding TargetDecision}" Height="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top" Tag="تفاصيل القرار الصادر في الجلسة..."/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Footer -->
                    <Border Grid.Row="2" Background="#F9FAFB" Padding="24" CornerRadius="0,0,12,12" BorderBrush="{StaticResource BorderGray}" BorderThickness="0,1,0,0">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Width="120" Height="40" Style="{StaticResource ActionButton}" Command="{Binding SaveSessionCommand}">
                                <TextBlock Text="حفظ" FontWeight="Bold"/>
                            </Button>
                            <Button Width="100" Height="40" Background="White" BorderBrush="{StaticResource BorderGray}" BorderThickness="1" Margin="12,0,0,0" Cursor="Hand" Command="{Binding CloseDialogCommand}">
                                <TextBlock Text="إلغاء" FontWeight="SemiBold" Foreground="{StaticResource TextDark}"/>
                            </Button>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
